# 依賴安全性評估與逐步升級策略報告

本報告針對 `GameServer` 目前使用的 Maven 依賴進行安全性審查。目前多個核心組件存在嚴重的安全漏洞（CVE），且版本落後多年。

## 1. 核心評估原則
- **Java 1.8 相容性**：所有建議版本必須支援 Java 1.8，並預留未來遷移至 Java 11/17/22 的路徑。
- **穩定優先**：每次僅升級一個組件，通過編譯並確認執行 Jar 正常後才進行下一個。
- **風險導向**：優先處理具有 RCE (遠端代碼執行) 風險的組件。

## 2. 依賴分析表 (完整清單)

| 優先序 | 組件名稱 | 當前版本 | 建議版本 (Java 8 最穩) | 安全風險 (CVE) | 升級難度 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | **Fastjson** | 1.2.76 | **Secure Shim** | **極高**: RCE 漏洞 (CVE-2022-25845)。 | **已移除** |
| **2** | **Log4j** | 1.2.15 | **Logback 1.2.13** | **極高**: EOL 且有反序列化 RCE。 | **已橋接** |
| **3** | **Mina Core** | 2.2.2 | **2.2.9** | **高**: 反序列化漏洞 (CVE-2024-52046)。 | 低 (已完成) |
| **4** | **C3P0** | 0.9.2.1 | **0.14.1** | **高**: JNDI 反序列化 RCE。 | 低 (已完成) |
| **5** | **Netty** | 3.6.2 | **4.2.16.Final** | **中**: 請求走私與 DoS 風險。 | **極高** (API 重大變動) |
| **6** | **MySQL Conn** | 5.1.15 | **8.4.0 LTS** | **中**: 屬性轉換 RCE (CVE-2023-21971)。 | 中 (已完成) |
| **7** | **JNA** | 5.8.0 | **5.16.0** | **中**: Windows 登錄檔讀取 DoS 漏洞。 | 低 (已完成) |
| **8** | **OSHI** | 5.7.5 | **7.4.2** | 低: 主要是 bug 修復與硬體支援更新。 | 低 (已完成) |
| **9** | **Commons-Log** | 1.1.1 | **1.3.4** | 低: 版本過老 (2007)，建議橋接至 SLF4J。 | 低 |
| **10** | **Servlet API** | 3.0.1 | **4.0.1** | 低: 主要是 EOL 與安全加固不足。 | 低 (已完成) |
| **11** | **Javolution** | 5.5.1 | 7.0.0 | 低: API 變動極大，原始碼無引用。 | **已註解** |
| **12** | **SLF4J** | 1.7.36 | 2.0.16 | 低: 性能優化。 | 低 (已完成) |

## 3. 技術分析專題

### 3.1 日誌系統的關係 (JCL vs Log4j vs SLF4J)
專案透過 `jcl-over-slf4j` 與 `log4j-over-slf4j` 橋接器，在不更動原始碼前提下，將所有日誌導向現代化的 **Logback** 引擎。

### 3.2 輔助工具類組件分析
- **JNA & OSHI**：兩者為相依關係，用於硬體綁定 (HWID)。升級 JNA 解決了特定的 DoS 風險，同步升級 OSHI 則確保了對現代硬體的採集準確性。
- **Fastjson Secure Shim**：透過在本地建立 `com.alibaba.fastjson` 的仿冒類別，徹底移除了外部依賴，解決了反序列化漏洞的根本問題。

## 4. 未來 Java 版本相容性提醒 (11/17/22)
- **Netty 4.x**：未來網路層升級的重頭戲，涉及代碼重寫。
- **MySQL Connector 8.4+**：已為 Java 22 做好準備。
- **Logback**：比 Log4j 更能適配 Java 11+ 的類別載入架構。

---
*最後修正日期：2026/08/02 (同步 JNA/OSHI 升級狀態)*
